# 当前进度（滚动文档）

> **给任何接手者（人或 AI）**：开工前先读本文件，它回答「做到哪、下一步是什么、卡在哪」。
> 收工前请更新它。不要从 `交接说明.md` 或 `docs/` 一篇篇考古——那是总纲与历史归档。
>
> 关联：AI 工具入口 `AGENTS.md` ・ 项目总纲 `交接说明.md` ・ 代码地图 `docs/01-项目架构与代码地图.md`

**最后更新**：2026-09-13
**当前基线**：v1.5.49（versionCode 69）— 已发布 GitHub Release
**工作区**：`C:\starword`（唯一，详见 `AGENTS.md` §0）

---

## 一、下一步（建议优先级，待开发者拍板）

1. **真机验证 v1.5.50 的 FOV 先验修复**（需实体手机）：用一张**竖拍**（EXIF Orientation=6/8）、
   且 EXIF 带焦距的手机星空照片，确认官方引擎能解出（§0.61 修复的正是竖拍照片
   识别必败的 bug；本次用户报告照片即竖拍 26mm 等效）。
2. **疏散星团暗星区（M44 类 / 秋季四边形类）** — 919 颗亮星表（HIP ≤5 等）在该区域星点过疏，
   自研引擎解不出（本次用户照片的秋季四边形天区 mag≤4.0 仅 9 颗，自研引擎 40/64/200 星全败）。
   先做「提示用户开启定位以加速识别」的文案与引导；星表扩到 HIP ≤7 等（约 9000 颗，
   三角形索引内存约 10 倍）作为**独立实验**，用远端评估内存与 APK 体积后再决定。
3. **AR 视场/地平线真机复测** — v1.5.47 的修复（磁偏角真北、3D 航向校准、地平线四边求交、
   主摄 FOV 优选）尚未在真机确认。**需实体手机，只能由开发者执行。**
4. **今日天象卡片** — 月相、行星可见性、当晚亮点。
5. **工程化**（CI 构建、F-Droid 发布准备）— 建议等有外部用户后再投入。
6. 可选细节：木星伽利略卫星、土星环倾角对星等的影响（v1.5.48 遗留）。

## 二、最近完成

- **2026-09-13（v1.5.50，待发版）** — 修复**竖拍照片 FOV 先验错配**（§0.61，用户报告）：
  - 现象：竖拍（EXIF Orientation=6/8）+ EXIF 有焦距的手机星空照片，官方引擎必败、
    自研引擎在秋季四边形等亮星稀疏天区也失败；对照 App 1s 解出。
  - 根因：`FovEstimate.fovDegFromFocal35(26, portrait=true)` 竖拍分支用 24mm 短边
    计算长边视场（49.55°），而传感器长边视场物理上恒为 67.3°（4:3 对角等效）；
    App 解码链路又不应用 EXIF 旋转 → 官方引擎按位图宽度 2200px 换算的 pixscale
    先验 [60.8, 109.5]"/px 恰好把真实值 110.1"/px 排除在外。
  - 修复：竖拍分支删除，长边视场恒按 36mm 计算（portrait 参数保留仅为兼容）。
  - 复现：用户照片 .gray 用项目自身匹配器 40/64/200 星全 UNSOLVED（本地复现）。
  - 单测更新：`StarSolverTest.fovFromFocal35_portraitSameAsLandscape`。
  - 详见 `docs/59-验证报告增补-§0.61-修复竖拍照片FOV先验错配v1.5.50.md`。
- **2026-09-13（v1.5.49 已发版）** — 构建并发布 v1.5.49：
  - 恢复 `testdata/` 回归素材（从 `C:\star\_archive\20260912-pre-merge\` 归档拷回），
    全量单测 **149 项 0 失败**（此前的 3 项素材失败随之消除，真实照片回归通过）；
  - debug 包（24.8MB）与 release 签名包（16.4MB，R8）均构建成功，
    签名证书与历史版本一致（CN=StarCam，SHA-256 `b04a854f…`，可覆盖升级）；
  - ⚠️ **本机 release 构建的两个坑**（本次实测）：① Gradle 配置缓存序列化
    `lintVitalAnalyzeRelease` 会尝试联网解析 `org.codehaus.groovy:groovy:3.0.21`，
    而本机 **Maven Central 不可达**（阿里云镜像可达），导致 release 构建失败；
    ② 解法为 `gradle :app:assembleRelease -x lintVitalRelease --offline`
    （debug 构建不受影响，正常联网命令即可）。
  - README 中英两版补 v1.5.49 说明并校正过期数字（测试 143→149、验证报告 48→50 份、
    章节至 §0.60、英文版梅西耶 45→44）；APK 归档到 `apk/`。
- **2026-09-12（v1.5.49 功能开发）** — 两项改动：
  - **§0.59 无 GPS 照片位置兜底**：照片缺 EXIF GPS 时用设备当前定位生成天区先验，
    官方引擎不必再从全天空盲解起步。三条硬边界——EXIF 定位永远优先、无拍摄时刻则不兜底、
    半套 GPS 视为缺失；位置来源在求解进度文案与结果页信息面板明示。
    日月行星标注（§0.58）与批量导出同步受益。
  - **§0.60 失败诊断图清晰度修复**（开发者报）：诊断图原先固定降采样到长边 800px，
    而原图可达 2200px，查看器放大后明显更糊；改为与原图同分辨率输出，圈半径 / 描边等比放大。
  - 单测 149 项（新增 `ExifPriorsFallbackTest` 6 项）。详见 `docs/57`、`docs/58`。
- **2026-09-12** — 把本机两份**无共同祖先**的副本（`C:\starword` 与 `C:\star\StarCam`）
  合并为**唯一工作区**：接入 GitHub 完整历史（6 提交 + `v1.5.26/37/39/48` 四个轻量 tag + origin），
  并吸收此前只存在于 StarCam 侧的 8 处修复 ——
  `WcsTransform.rescaledFor()` 的 CRPIX/CD 缩放修正（星图错位根因）、`ResultScreen`/`OverlayRenderer`
  改用它、`allowBackup=false`、在线求解上传副本清理、分析帧位图回收、OkHttp `use{}`、CD 缩放回归测试。
  旧副本与冗余归档在 `C:\star\_archive\20260912-pre-merge\`。
  新增 `AGENTS.md`（AI 工具统一入口）与 `交接说明.internal.md`（内部稿，不入库）；校正 `交接说明.md` §十。
- **2026-09-11** — v1.5.48 发布到 GitHub Release（APK 附件 + 轻量 tag）。
- **2026-09-10** — v1.5.48 月亮与行星实时标注（§0.58，`docs/56`）；仓库首次纳入 git。

## 三、阻塞 / 待开发者处理

- [ ] **astrometry.net API key 轮换** —— 该 key 自开源首发提交 `511c825` 起就在公开历史中，
      改历史无用，只能去 nova 侧作废并更换。
- [ ] **真实照片回归素材不在工程内** —— `RealPhotoMatchTest` / `Photo12RegressionTest` 依赖
      `testdata/realphotos` 与 `testdata/gray12`，这批素材目前在归档目录
      （`C:\star\_archive\20260912-pre-merge\starcam-bundle\testdata\`），导致本机跑不了真实照片回归。
      建议恢复到 `C:\starword\testdata\`（`.gitignore` 已排除 `testdata/`，无入库风险）；
      临时替代：跑测试时用 `PHOTO_DIR` / `PHOTO12_DIR` 指向归档。
- [ ] **AR 视场/地平线真机复测**（需实体设备）。
- [ ] 中间提交 `f14b268`（WIP 重建）自身不编译，HEAD 已修复。
      如需整理历史：**本环境 `git rebase` 有删除 `.git` 的前科，务必先备份**。

## 四、与远端的关系

本地 `main` 领先 `origin/main`（未推送的提交会在这里累积）。**本文不写死数字**，以命令为准：

```bash
cd C:/starword
git fetch origin                      # 若提示不认识 origin/main，先执行这句（新克隆的目录通常已有）
git status -sb                        # 看 ahead / behind
git log --oneline origin/main..HEAD   # 看待推送的具体提交
```

推送前请确认：本地是否还有不想公开的内容（本机凭据路径、内部稿、机器相关配置）。

---

## 更新规约

1. 每次实质工作结束（改代码 / 定方案 / 发版 / 合并）后更新本文件：
   **补「最近完成」，并据实调整「下一步」**。
2. 「最近完成」保留最近 3~5 条即可，更早的删掉（历史在 `git log` 与 `docs/` 里）。
3. 只写**状态与决策**，不写过程细节；过程细节进 `docs/NN-验证报告增补-§0.xx-主题.md`。
4. 本文件与 `AGENTS.md` 一样，是**跨工具接力**的载体，务必提交进 git。
