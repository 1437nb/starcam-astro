# 验证报告增补：§0.56 回滚 v1.5.43（找星导航/点击详情）v1.5.45

## 一、背景
用户判定 AR 抖动由 v1.5.43（AR 找星导航 + 点击详情）引入，要求回滚后重做。
经代码核对确认：
- 基础星图渲染路径（LayeredRenderer.draw 调用参数）v1.5.43 与 v1.5.42 **逐位一致**；
- v1.5.43 新增开销集中在导航叠加层（每帧新建 5 个 Paint + 多次小数组分配，
  仅在有目标时执行）与 ArSkyProjector Frame 重构的每帧 Frame/a 分配；
- v1.5.44 互补滤波抖动修复独立于 v1.5.43（用户决定保留）。

## 二、回滚内容（= v1.5.42 功能态 + 保留 v1.5.44 融合）
1. `astro/ArSkyProjector.kt`：删除 Frame/buildFrame/projectPoint，`project()`
   恢复 v1.5.42 内联形态（A 矩阵/f/cx/cy/校正偏移/local enuToCam，数学逐位不变，
   含 §0.53 belowHorizon 全天星空）；
2. `ui/camera/CameraScreen.kt`：删除找星/点击详情全部代码——
   状态变量、arHitTest、对焦层命中分支、AR 画布导航叠加、ObjectInfoCard 浮层、
   🎯 按钮、🌌 开关重置行、目标选择弹窗调用；
3. 删除 `ui/camera/ArTargetPicker.kt`；
4. `ui/I18n.kt`：删除 I18n.Ar §0.54 五条文案；
5. `ArSkyProjectorTest`：删除依赖 projectPoint 的 4 项测试；
6. **保留**：AttitudeFusion / QuatMath / DeviceOrientationTracker 融合
   （v1.5.44 互补滤波）。

## 三、验证
- `compileDebugKotlin` 通过；`testDebugUnitTest` 全量回归通过；
- `assembleDebug` 成功：`StarCam-v1.5.45-回滚找星导航-debug.apk`（仅 debug）。
- 真机复测：AR 星图与 v1.5.42 行为一致（找星/点击详情入口已移除），
  抖动应消失（叠加互补滤波后更稳）。
