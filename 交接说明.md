# StarCam / 星空识星 — 项目交接说明

> **交接时间**：2026-09-10
> **交接对象**：接手本项目的下一位 AI 开发者
> **版本基线**：v1.5.48（versionCode 68）
> **工程根**：本文件夹 `C:\starword`（由原 `C:\starcam-bundle` 拷贝而来，不含构建产物与隐私数据）

---

## 一、项目是什么

**StarCam / 星空识星**（包名 `com.starcam.astro`，用户亦称「爱观天」）是一款**纯离线优先**的 Android 天文摄影与星空识别 App：

- 拍摄或导入夜空照片 → **本地盲解天区**（RA/Dec、视场、旋转、parity）→ 在照片上精确叠加星座连线、恒星专名与梅西耶深空天体标注。
- 三层求解引擎：官方 `astrometry.net` 0.97（NDK 交叉编译 .so，主引擎）→ 自研 8400+ 星表三角形投票匹配器（宽场补充）→ 可选在线 `nova.astrometry.net` API 兜底（用户自行配置 API Key）。
- 另有 **AR 实时星图**：传感器驱动（旋转矢量 + 陀螺仪互补滤波 + 定位），把真实星空按方向投影到相机取景画面上，支持全天星空模式、地平线罗盘、找星导航。
- 技术栈：Kotlin + Jetpack Compose（Material3）、CameraX 1.3.4（Camera2 Interop）、OkHttp、纯 JVM 数学对象（可单测）。
- 许可证：GPL v2.0。

---

## 二、目录结构

```
C:\starword\
├── code\                    # Android 工程源码（仅源码 + Gradle 配置，约 18M）
│   ├── app\
│   │   ├── build.gradle.kts     # 版本/签名/APK 命名规则（updateDesc）
│   │   ├── proguard-rules.pro
│   │   └── src\
│   │       ├── main\
│   │       │   ├── java\com\starcam\astro\   # 全部 Kotlin 源码
│   │       │   ├── assets\indexes\            # astrometry.net 离线 FITS 索引（11M）
│   │       │   ├── assets\offline_photos\     # 12 张离线演示照片（降采样版）
│   │       │   ├── jniLibs\arm64-v8a\         # libstellar_solver.so（5.1M）
│   │       │   ├── cpp\astro_bridge.c         # JNI 桥
│   │       │   └── res\ + AndroidManifest.xml
│   │       └── test\java\                     # 16 个单元测试文件（142 项）
│   ├── build.gradle.kts / settings.gradle.kts / gradle.properties
│   └── tools\diag_p12.txt      # 诊断日志（非证书）
├── docs\                    # 57 篇中文文档（00 总览 ~ 56 §0.58 验证增补）
├── tools\                   # 24 个 Python 工具脚本（星表生成/定标/连线/评测）
├── apk\                     # 最新 2 个可安装验证基线（v1.5.47 debug + v1.5.39 release）
├── indexes\                 # 8 个 FITS 索引副本（服务器端 solve-field 定标用）
├── README.md  LICENSE  server-setup.md  .gitignore
└── 交接说明.md               # 本文件
```

**刻意不打包**（本机 C:\starcam-bundle 仍有）：`testdata/`（用户私拍原图 + .gray，隐私红线）、历史 54 个 APK、`code` 下全部构建产物（`app/build`、`.gradle`、`.kotlin`）、`local.properties`（机器相关）。

---

## 三、代码地图

`com.starcam.astro` 包结构（`code\app\src\main\java\com\starcam\astro\`）：

| 目录 | 职责 | 关键文件 |
|---|---|---|
| `astro\` | 核心天文数学与引擎（纯 JVM 可单测） | `StarSolver.kt`（三层引擎调度）、`StarCatalogData.kt`+Part1~8（8400+ 星表）、`LocalStarMatcher.kt`（实时匹配）、`SkyEphemeris.kt`（LST/坐标转换）、`ArSkyProjector.kt`（AR 针孔投影）、`LayeredRenderer.kt`（统一标注渲染）、`AttitudeFusion.kt`（姿态互补滤波）、`CameraMath.kt`（FOV/滚转/旋转）、`QuatMath.kt`、`MessierCatalog.kt`、`StarNames.kt/StarNamesEn.kt`、`StellarSolverNative.kt`（JNI）、`WcsTransform.kt`、`PlateSolveClient.kt`（在线 API） |
| `data\` | 持久化 | `SettingsRepository.kt`（含 apiKey/arFovDeg 等）、`HistoryStore.kt`、`StatsStore.kt` |
| `ui\` | Compose 界面 | `StarCamApp.kt`（导航）、`camera\CameraScreen.kt`（**最大最复杂**，相机+AR）、`result\ResultScreen.kt`、`home\HomeScreen.kt`、`settings\SettingsScreen.kt`、`batch\`、`gallery\`、`history\`、`demo\OfflineDemoScreen.kt`、`I18n.kt`（中英双语字典）、`theme\` |
| `util\` | 设备能力 | `DeviceOrientationTracker.kt`（传感器姿态）、`LocationHelper.kt`、`ImageUtils.kt` |
| 顶层 | | `MainActivity.kt`、`AutoTest.kt` |

**核心数据流（识别）**：拍照/选图 → 提星（detectStars）→ 官方 .so 盲解（带天区先验加速）→ 失败则自研三角形投票匹配 → 再失败则在线 API → `SolveResult`（RA/Dec/pixScale/orientation/parity/WCS）→ 叠加渲染/保存。

**核心数据流（AR）**：`DeviceOrientationTracker`（旋转矢量锚点 + 陀螺积分 + 24ms 外推）→ `DevicePointing(right,up,axis)`（真北东北天基）→ `ArSkyProjector.project` 针孔投影 → `LayeredRenderer` 绘制 + 地平线罗盘 + 找星导航。

---

## 四、版本沿革（关键迭代一览）

| 版本 | 内容 |
|---|---|
| v1.5.2~1.5.30 | 三层引擎落地、离线识别、星表扩容（8400+）、星座/星名/梅西耶标注、中英文国际化 |
| v1.5.31 | 方向传感器辅助粗定标（陀螺仪航向 + 定位粗指向） |
| v1.5.32~34 | 图层控制 + 天体科普卡片 + 星座变形/预览叠加修复 |
| v1.5.35 | AR 实时星图上线 |
| v1.5.36~37 | 拍照闪退修复、AR 射线扇面修复 |
| v1.5.38 | Pro 手动曝光 + 电子水平仪 + FOV 自动标定 |
| v1.5.39 | 星座连线对齐 Stellarium（constellationship.fab 数据） |
| v1.5.40~42 | AR 姿态平滑预测、全天星空模式、HUD 朝下显示坐标 |
| v1.5.43~46 | AR 找星导航 + 点击详情（v1.5.43 引入抖动 → 回滚 v1.5.45 → 零分配重做 v1.5.46） |
| **v1.5.47** | **修复 AR 视场缩放与地平线贴合**：磁偏角真北对齐、全天球 3D 航向校准取代 2D 拉扯、地平线 4 边求交 + 8 方位罗盘、主摄 FOV 优选、CameraX 4:3 锁定 |
| **v1.5.48** | **新增月亮与行星实时标注**（§0.58）：JPL 近似根数算日月行星地心位置 + Meeus 月球级数、站心视差修正、统一 J2000 历元（扣黄经岁差）、识别图/AR/批量导出三处同步标注、日月按真实视直径绘制、新增「月亮与行星」图层开关与科普卡片 |

详细过程见 `docs\`（00~56 共 57 篇，编号即 §0.xx 章节，最新 §0.58 在 56 号文档）。

---

## 五、构建与测试（本机 Windows）

```bash
cd C:\starword\code
gradle :app:assembleDebug          # 构建 debug APK
gradle :app:testDebugUnitTest      # 全量单元测试（142 项）
```

- 环境：OpenJDK 17、Android SDK 34、Gradle 8.12.1。
- **`local.properties` 需按本机 SDK 路径重建**（交接包刻意未带，属机器相关文件）：
  本机实测 SDK 在 **`C:\dev\android-sdk`**（含 `platforms/android-34`、`build-tools/34.0.0`），
  不是默认的 `%LOCALAPPDATA%\Android\Sdk`。写 `sdk.dir=C:/dev/android-sdk` 即可。
- 架构：native 引擎仅 arm64-v8a（需 64 位真机；x86 模拟器走 JVM 引擎回退）。
- 单测是纯 JVM 的（不依赖 Android）。**注意 `--rerun-tasks`**：不加时 Gradle 可能
  直接 `FROM-CACHE` 复用上次结果、等于没跑（见 `docs\05` §5.3 坑 5）。
- ⚠️ **不要把 `testDebugUnitTest` 与 `assembleDebug` 放进同一次 gradle 调用**（2026-09-10 实测）：
  两者都要读写 `app\build\tmp\kotlin-classes\debug`，并行执行时会互锁，报
  `dexBuilderDebug` / `mergeDebugJavaResource` 处理 `.dex`/`.jar` 失败
  （表现为「安全策略拒绝读/写」，极易误判为环境限制）。**拆成两条命令串行执行即正常**：
  ```bash
  gradle :app:testDebugUnitTest      # 先测
  gradle :app:assembleDebug          # 再打包
  ```
- 一次全量 Kotlin 编译约 11~14 分钟（4 核 8G 机器）；增量编译约 1~5 分钟。
  **不要并行发起多个 gradle 任务**，会抢守护进程互相拖死。

---

## 六、长期开发约束（用户硬性要求，必须遵守）

1. **APK 命名铁律**：每次新版本，APK 文件名必须自动带本版核心更新内容简述。已由 `app\build.gradle.kts` 的 `updateDesc` 属性驱动，命名格式 `StarCam-v{versionName}-{更新内容}-{variant}.apk`。**发新版时必须同步改 `versionCode`、`versionName` 与 `updateDesc`**。
2. **Release 只按需跑**：日常 bug 修复/功能迭代**只构建 debug 验证版，绝不自动跑 `assembleRelease`**；仅当用户明确说「跑 release」才产出正式签名包。
3. **隐私红线**：`testdata/`（用户私拍原图、.gray 原始数据）**绝不提交/打包/分发**；文档中服务器一律用占位符 `<build-server-ip>`，SSH 一律用别名（如 `aa`），不写真实 IP 与密码。
4. **敏感凭据外部化**：任何 API key/密码不得硬编码进源码或文档（见下节 §7）。

---

## 七、凭据与运行时约定

- **外部化原则**：任何新增脚本/文档禁止出现真实密钥明文。需要凭据的脚本一律从环境变量或命令行参数读取——例如 `tools/nova50b.py` 从环境变量 `NOVA_API_KEY` 或命令行 `--key <key>` 注入，未配置时明确报错退出。
- **App 端设计**：在线模式 API key 由用户在「设置」中输入、存于 `SharedPreferences`（无默认值），不随包分发。
- **签名凭据**：release 签名从工程目录之外的本机文件读取，不打包分发、不入库。

---

## 八、下一步候选方向（供接手参考）

1. **AR 视场/地平线真机复测**（v1.5.47 修复，仍需真机确认视场缩放、地平线罗盘与星图贴合）。
2. ~~月亮/行星实时标注~~ → **已于 v1.5.48 完成**（§0.58）。遗留可做：
   - 无 GPS 照片的位置兜底（用当前定位并明确提示「位置来源为当前定位」）；
   - 木星伽利略卫星、土星环倾角对星等的影响。
3. 今日天象卡片（月相、行星可见性、当晚亮点）。
4. 观星足迹地图（按历史识别记录绘制观测地点）。
5. 星表继续扩容（NGC 天体）。
6. 工程化：CI 构建、F-Droid 发布准备。

---

## 九、给接手 AI 的快速上手清单

1. 先读 `docs\00-总览与接手指引.md`（项目全景）与 `docs\01-项目架构与代码地图.md`（代码地图）。
2. 通读 `code\app\src\main\java\com\starcam\astro\astro\` 下的核心数学对象（SkyEphemeris / ArSkyProjector / CameraMath / AttitudeFusion）——它们是纯 JVM 的，读起来最省力。
3. 动手前先跑一次 `gradle :app:testDebugUnitTest --rerun-tasks` 确认基线全绿（142 项）。
4. 改动后**只构建 debug**，按 §6 更新版本号与 updateDesc，APK 会自动带更新内容命名。
5. 遇到 AR/相机相关改动，注意 `CameraScreen.kt` 是超大文件（1500+ 行），其内部按 `§0.xx` 注释分区，改动时保持零每帧分配原则（Paint/数组在 `remember` 一次性创建）。

---

## 十、版本控制

本仓库为 git 仓库，分支 `main`，**同时就是 GitHub 的克隆**：
远端 `https://github.com/1437nb/starcam-astro.git`，历史 v1.5.26 → v1.5.48
（含 `v1.5.26` / `v1.5.37` / `v1.5.39` / `v1.5.48` 四个轻量 tag）。
具体提交哈希见 `git log`，本文不写死以免过期。

> **2026-09-12 合并说明**：此前本机存在两份**无共同祖先**的副本（本目录，与另一份 GitHub 克隆），
> 换工具打开不同目录就会在分叉的基线上继续改，是「切换环境即丢进度」的根源。
> 现已合并为**唯一工作区**：只在本目录开发，**不要再创建第二份副本，也不要从别处手工同步**。

- **提交者身份**：`user.name=1437nb`，邮箱使用 GitHub 提供的 `noreply` 地址（公开仓库不留真实邮箱）。
  全局 git 身份未配置，故只设在本仓库（`git config`，非 `--global`）。
- ⚠️ **改变提交历史前必须先做物理备份**。本环境实测出现过执行 `git rebase --root`
  后整个 `.git` 目录被删除的情况（工作区文件未受影响，但提交全部丢失，只能重新 init）。
  如需改写历史，请先 `cp -r .git .git.bak` 或直接压缩备份整个目录。
- **纳入范围**：198 文件 / 19MB。含 `code/`（源码 + 8 个 FITS 离线索引 + `.so` + 演示照片）、
  `docs/` 57 篇、`tools/` 24 个脚本。
- **红线（`.gitignore` 已强制）**：`testdata/`（私拍原图）、`local.properties`（机器相关）、
  签名密钥、`/apk/` 产物、`**/build/`、`.workbuddy/`、内部交接稿。**请勿用 `git add -f` 绕过。**
- ⚠️ **`.gitignore` 的两个锚定陷阱（已修复，勿改回去）**：
  - `indexes/` 会匹配任意层级同名目录，把 `code/app/src/main/assets/indexes/`
    那 8 个 FITS **离线索引一并排除**（离线求解的必需资产，漏提交则克隆后无法识别）。
    必须写成 `/indexes/`。`apk/` 同理，已改 `/apk/`。
  - 新增 `.gitattributes` 显式标记 `.so/.fits/.jpg` 等为 `binary`。
    **本仓库含多种二进制资产，缺少该文件会被 git 的换行转换损坏，务必保留。**
- 工程根另有 `AGENTS.md`（给任意 AI 开发工具的统一入口，含构建命令与硬性约束）
  与 `交接说明.internal.md`（内部稿，含真实联系方式等，已在 `.gitignore` 中排除，切勿提交）。
