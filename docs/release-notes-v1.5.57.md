# StarCam v1.5.57 —— 索引内存可归还 + release 构建修复

## 本版内容

### 索引内存可归还（§0.67）

系统内存紧张时，主动归还约 11MB 的星表索引缓存，降低被系统杀后台的概率。

- 求解线程状态从**全局单例**改为**每次求解一个实例**，修掉 §0.65 遗留的
  use-after-free 隐患（上次超时泄漏线程、本次正常结束时，调用方会对仍被读取的
  内存执行 `solver_free`）。
- 新增 `releaseIndexes()` JNI 接口 + `StarCamApplication.onTrimMemory` 钩子：
  系统报 `TRIM_MEMORY_RUNNING_LOW` 时归还 8 档索引。
  阈值选在此处而非更激进，是为避免用户连续识别时把索引丢掉、下次反而变慢。
- 释放前检查有无超时 detach 线程在跑，有则拒绝释放（宁可占内存也不崩）。

### 修复 release 构建失败（§0.68）

**v1.5.56 与 v1.5.57 此前都打不出 release 包**（日常只构建 debug，所以一直没暴露）。
根因：§0.66 引入加密存储后，Tink 引用的 `com.google.errorprone.annotations.*`
是编译期注解、不随运行时依赖发布，R8 的 "Missing classes" 检查把它当致命错误。
本版追加 `-dontwarn` 规则修复。**本 APK 是签名包**（证书与历史版本一致，可直接覆盖升级）。

## 安装包

| 文件 | 大小 | 说明 |
|---|---|---|
| `StarCam-v1.5.57-...-release.apk` | 16.5 MB | **推荐**。R8 优化，签名（v2 scheme），可直接覆盖旧版 |
| `StarCam-v1.5.57-...-debug.apk` | 26.9 MB | 调试包，含额外日志，用于问题排查 |

- 版本号：versionCode 77 / versionName 1.5.57
- 签名证书 SHA-256：`b04a854f…`（CN=StarCam，与 v1.5.49~v1.5.55 一致）
- 运行要求：Android 8.0 (API 26) 及以上

> ⚠️ 关于 v1.5.55：该版本的 release APK 存在未签名问题（装不上），请直接使用
> v1.5.57，或改用 v1.5.55 的 debug 包。

## 验证

- 单元测试 **145 项 / 15 个测试类 / 0 失败**
- 真实照片回归：南宁 74° 宽场照片 SOLVED（15 内点）、apod4 北斗 SOLVED、
  6 张已知假阳性样本保持 UNSOLVED
- release APK 签名经 `apksigner` 验证通过，并与历史版本证书指纹比对一致

## 已知问题与下一步

- **真机验证待做**：§0.65 修复的「官方引擎提星恒为 0」需在实体手机上复测
  （原图识别、相册高对比度图识别、离线演示 12 张的耗时）。
- **宽场投票轮净化**（§0.62 遗留）：60°+ 宽场的逐星投票仍会被伪三角形污染，
  目前靠打分轮兜底。原计划的「改用三角形内角做索引键」经复核**前提不成立**
  —— gnomonic(TAN) 并非保角映射（保角的是 stereographic），内角在投影下会变。
  实测内角键把严格窗命中率从 30.1% 提到 49.4%，有收益但非根治，待进一步评估。
- **AR 视场/地平线**：v1.5.47 的修复尚未在真机确认。
