# 当前进度（滚动文档）

> **给任何接手者（人或 AI）**：开工前先读本文件，它回答「做到哪、下一步是什么、卡在哪」。
> 收工前请更新它。不要从 `交接说明.md` 或 `docs/` 一篇篇考古——那是总纲与历史归档。
>
> 关联：AI 工具入口 `AGENTS.md` ・ 项目总纲 `交接说明.md` ・ 代码地图 `docs/01-项目架构与代码地图.md`

**最后更新**：2026-09-20
**当前基线**：v1.5.61（versionCode 81）— SEP 提星阈值语义修正（§0.72）
**工作区**：`C:\starword`（唯一，详见 `AGENTS.md` §0）
**构建环境**：**全部在本机 Windows 完成**（2026-09-17 起，开发者要求）。`.so` 用
`tools\build_so.bat`，APK/单测用本机 gradle；不再依赖远端构建服务器。

---

## 一、下一步（建议优先级，待开发者拍板）

1. **窄场能力是否要投入**（§0.73 已探底，**需开发者拍板**）：
   当前能力边界约 ≥15~20° 视场；0.5°~10° 的望远镜/长焦场景 17/18 失败，
   根因是星表深度（生产索引只用 mag≤4 的 514 颗星）而非匹配器逻辑。
   要支持窄场必须做「预构建序列化索引 + 分级索引」（亮星快路径 + 暗星慢路径），
   否则 mag 加深到 6.5 等时索引构建 41s、500 万条记录、512MB 堆跑不完。
   **产品上是否需要望远镜场景，决定这个工程做不做。**
2. **求解耗时** —— §0.74 已做主投票轮优化（46.6s → 13.8s，3.4 倍，行为逐位不变）。
   剩余耗时结构：detect 0.13~0.39s + 主投票轮 0.27~1.9s + 兜底阶梯 + 打分轮
   （有 3s 墙钟预算兜底）。若还要再压，方向是让 `scoredRound` 与投票轮并行，
   或先用最大三角形边长估视场先验直接路由。
3. **真机验证 v1.5.61**（需实体手机）：§0.72 的 SEP 阈值修复尚未在设备确认。
   请用 v1.5.61 重导 9-17 原图，识别日志现有提星诊断（星数/边界剔除/flux 范围/
   边缘占比/重合率）。
4. **宽场投票轮本身的净化**（§0.62 遗留）：打分轮是兜底，投票轮在 60°+ 宽场仍被伪三角形污染。
   **本次已把立论前提查clean了**（见下），原「索引改存三角形内角」的方案**不成立**：
   gnomonic(TAN) **不是保角映射**（保角的是 stereographic），径向尺度 sec²θ、切向 secθ，
   各向异性 = secθ（θ=30° 时 1.155、θ=40° 时 1.305）→ 内角在投影下**会**变。
   纯投影层实测（南宁照，画面内 mag≤4.5，按离画面中心角距分箱）：

   | 离中心 | 归一化比值键（现状）严格窗命中 | 内角键严格窗命中 |
   |---|---|---|
   | <8° | 71.7% | 83.5% |
   | 8~16° | 38.7% | 63.2% |
   | 16~26° | 24.9% | 47.8% |
   | ≥26° | 18.8% | 28.9% |
   | **合计** | **30.1%**（中位失真 0.01996） | **49.4%**（中位 0.01224） |

   即**内角键确实优于比值键（1.6 倍），但不是"不变"，只是畸变更小**。真正的价值在于
   它对检测定位误差的敏感度不同 —— 待补测后决定是否值得重建索引（全量重建 + 回归成本高）。
   相关探针脚本在 `C:\dev\harness\`：`clean_distort.py`（分箱畸变）、
   `conform_check.py`（保角性分层验证）、`minimal_proj_check.py`（最小验证）。

3. **AR 视场/地平线真机复测** — v1.5.47 的修复（磁偏角真北、3D 航向校准、地平线四边求交、
   主摄 FOV 优选）尚未在真机确认。**需实体手机，只能由开发者执行。**
4. **今日天象卡片** — 月相、行星可见性、当晚亮点。
5. **工程化**（CI 构建、F-Droid 发布准备）— 建议等有外部用户后再投入。
6. 可选细节：木星伽利略卫星、土星环倾角对星等的影响（v1.5.48 遗留）。

## 二、最近完成

- **2026-09-20（§0.74 投票轮提速 3.4 倍）** — **识别耗时大幅下降，行为逐位不变**：
  - **实测基线**：12 张宽场回归合计 46.6s（单张 1.6s~11.1s），分段计时显示
    detect 只占 5~10%，**主投票轮占 1.4~4.4s** 才是瓶颈（打分轮反而不是 ——
    5092 的打分轮只花 1.3s，5087 仅 0.3s）。
  - **根因（计数器实测）**：一轮投票要扫 **5,966 万条目**、往 HashMap 插
    **394 万次**，而最终只取 7.3 万个候选 —— 即每个三角形扫 14,736 条、
    插 972 次、只要 18 个。两个浪费来源：
    ① 索引把每个三角形存进 **9 个相邻桶**（`quantizeKeys` 默认 window=1），
    查询扫 729 个桶时同一对象被重复见到 9 次；
    ② 候选合并用「全量进 HashMap + 全排序取 18」。
  - **修复**：① triMap 改为**每三角形只存一个桶**（索引 305,118 → ~34,000 条），
    各查询点 window 同步 +1 以保持覆盖范围不变（原「存 ±1 + 查 ±13」等效 ±14）；
    ② 改为**一次扫描 + 有界插入排序**，并把计数器从内循环的原子自增改为局部累加。
  - **两处必须精确复现的旧语义**（否则候选集变、解算结果变）：
    ① `merged[triKey] = t` 是**后写覆盖** —— 同三角形的重复记录里，最后扫描到
       的那个实例参与排序，而不同锚点插入的实例 `(hipA,hipB,hipC)` 排列不同、
       正是排序的第 3~5 键。改成"保留首个"实测让 4984 从 51 内点掉到 15、
       5068 从 50 掉到 13；
    ② 距离门槛必须用**严格大于** —— 旧算法是全排序后取前 cap，打平时按 hip 决胜。
  - **结果**：合计 **46.6s → 13.8s（3.4 倍）**，单张最快 361ms、最慢 2.6s。
    **12 张宽场内点数与基线逐个一致**（15/35/51/51/16/48/49/9/50/49/25/24），
    12/12 真解 0 假解；窄场 17/18 未解的判定也逐条不变。
    计数器保留为 `debugVoteScanTake()`（扫多少桶/条目/合并多少），定位同类问题可直接复用。
  - 新增 `PhaseTimingBench`（分段时间 + 扫描计数，无断言；CI 用 `skipPhotoTests` 排除）。

- **2026-09-20（§0.73 窄场覆盖盲区实测）** — **窄场/中场识别能力探底**：
  - **素材**：从 SkyView 拉 DSS 巡天图（FITS 自带 TAN WCS，真值免费），18 个视场
    0.5°~30°，含球状星团/星云/银河/深空/高纬/RA 接缝。工具
    `tools/fetch_narrowfield.py` + `tools/convert_narrowfield.py`，
    回归台 `NarrowFieldRegressionTest`（只报告、不设合格线）。
  - **结果：17/18 未解出**，唯一解出的是 30° 视场（9 内点）。
  - **根因（三层，逐层实测排除）**：
    1. **星表深度**：生产索引 `PROD_CATALOG_MAG = 4.0`，只纳 514 颗星、
       面密度 0.0125/deg²。而星表本身有 8415 颗到 6.5 等（0.204/deg²，16 倍）。
       实测各视场画面内 mag≤4 星数：≤5° 视场 0~1 颗、10° 视场 0~1 颗、
       30° 视场 6 颗 —— **连 8 颗都凑不齐，三角形匹配在数学上不可能**。
    2. **检测星与星表不重合**：正投影诊断显示检测到的 64 颗星里，落在星表上的比例
       30° 场 46.9%（解出）、10° 场 19~31%（失败）、≤5° 场 0~8%（失败）。
       即检测端比星表深得多，多出来的星全是无法匹配的噪声。
    3. **加深星等不可行（硬约束）**：实测索引规模与构建耗时 ——
       mag≤4.0 → 305,118 条 / 601ms；mag≤6.5 → **5,000,886 条 / 41.2s**（16.4 倍），
       且 6.5 等的索引在 512MB 测试堆里连求解阶段都跑不完。
       即「改一个常量」这条路被内存和构建时间双重堵死。
  - **结论**：当前能力边界约 **≥15~20° 视场**。窄场（望远镜/长焦）需要的是
    **预构建并序列化索引**（避开 41s 构建）+ **分级索引**（亮星快路径 + 暗星慢路径），
    是一个独立工程项，不是常量调整。是否投入取决于产品是否要支持望远镜场景。
  - **附带的流程发现**：`RealPhotoMatchTest.knownFalsePositivesRemainUnsolved`
    **在干净 HEAD 上就失败**（apod5 从 UNSOLVED 变为 SOLVED，ra=90.27 dec=4.68
    fov=71.6° 31 内点）。CI 因 `skipPhotoTests` 排除该测试类，从未发现。
    该解**高度可能正确**（31 内点 vs 旧错解 5；画面内 65 颗 4 等以上星；
    银道坐标 l=260° b=−1.5° 正处银河盘面），但 SkyView 实图比对未跑通，
    **尚未证实**。测试注释本身规定此种情况应改为真值断言而非删测试。

- **2026-09-20（安全加固）** — **凭据扫描能力补强**：
  - CI 的 gitleaks 走默认规则集，只匹配「已知凭据格式」（AWS AKIA…/ghp_…/私钥块等），
    「自定义变量名 = 任意口令」不在覆盖范围，会静默放行。
  - 新增 `.gitleaks.toml`（三条项目专属规则 + 占位符白名单）、
    `tools/check_secrets.py`（不依赖二进制的本地扫描器，支持 `--staged`）、
    `.githooks/pre-commit`（已实测拦下含口令的提交）。
  - `tools/gh_api_push.py` 推送前强制自检 —— 它是绕过所有 git 钩子的唯一推送通道。
  - `tools/gh_socks_tunnel.py` 的凭据改为从环境变量 / `~/.starcam/ssh-tunnel.env` 读取；
    `.gitignore` 补 `*.env` 等防线。
  - **踩坑记录（gitleaks 两个硬约束）**：① 它走 **RE2 引擎，不支持 lookahead**
    （`(?!...)` 会让它启动即 panic、CI 8 秒失败）——排除项必须写进 `allowlist.regexes`；
    ② 规则的字符类必须排除 `
`，否则 `"password="` 这类字面量的引号会与下一行
    引号配对，把两行正文糊成一次"赋值"匹配。两处都已修复并加了本机 gitleaks 实测
    （仓库零发现 + 构造样本 3/3 命中）。
  - **踩坑记录（gh_api_push.py 静默丢 workflow 文件）**：该脚本原先**无条件跳过**
    `.github/workflows/` 下的改动，于是 CI 配置在本地改了、提交也生成了，远端却
    停在旧版——而输出只打一行"被 GitHub 拒绝"，看起来像 token 问题。实测当前 token
    的 `x-oauth-scopes` 是 `repo, workflow`，GitHub 早就允许写 workflow 了。
    已改为探测 scope 后再决定跳不跳；本次用它把 CI 注释里的事故叙事清掉并推送成功。

- **2026-09-20（v1.5.61）** — **修复 SEP 提星阈值语义误用（§0.72，用户报告）**：
  - **问题**：用户装 v1.5.60 后用相册导入 9-17 原图仍失败（`work=6 votedStars=0`），
    但**同一张照片的高对比度版能识别**。
  - **定位**：把用户 report.json 里的 172 颗星直接喂给匹配器，**逐字复现失败** →
    证明失败在提星环节。四组实验证实这 172 颗是伪影：0/172 落在亮源上（真星处
    灰度 255、它们最高 94）、任意翻转/缩放变换均无法对齐、真值 WCS 下 0/172 对齐；
    73% 落在 30px 边界带（面积占比仅 6.3%，富集 11.6 倍）、最大 flux 96.7（真星 4143）、
    含 3 颗负 flux。
  - **根因**：`sepDetectStars(sigma=2.0)` 的参数语义是「背景 sigma 倍数」，但 C 层
    直接写进 simplexy 的 `plim`。而 plim 是**峰值显著度**，检出限
    `limit = sigma/(2√π·dpsf) × plim`，dpsf=1.0 时 plim=2.0 只相当于 **0.56σ**。
    实测用户照片：过阈像素 **29.27%** → 连通域被连成 **848,846 px 巨块** →
    超 `maxsize=2000` 被判「延展天体」整体丢弃 → 真星全丢，只剩边界伪影。
  - **修复**：① C 层换算 `plim = N × 2√π × dpsf`（2σ→7.09≈上游默认 8.0）+ 下限 4.0；
    ② Kotlin 侧边界过滤（16px，正常样本仅损失 2.5~2.9%）；
    ③ 提星质量核验（与 box-blur 前 30 亮星重合率 <30% 则回退 box-blur）；
    ④ **提星诊断写入识别日志**（星数/边界剔除/flux 范围/边缘占比/重合率）——
    用户要求"把识别过程记录下来"。
  - **验证**：过阈像素 29.27%→0.63%、最大连通域 848,846→55 px、单测 **170/170**、
    照片回归无回退。详见 `docs/70-…§0.72…`。
  - **待真机确认**：请用 v1.5.61 重导 9-17 原图；日志会记录提星诊断。

- **2026-09-19（v1.5.60）** — **修复 9-17 照片识别失败与旧照片回归（§0.71，用户报告）**：
  - **问题**：用户 2026-09-17 竖拍星空照片识别不出（附失败日志 + 原图）；
    修复过程中 9-12 旧照片（横拍）又出现 SOLVED→UNSOLVED 回归。
  - **根因（四层叠加，逐层二分确认）**：
    1. **投票阈值量纲错配**（§0.64 的尾巴）：900/60/25 绝对阈值按 box-blur 量纲
       标定，SEP flux 路径下主阈值只放 2 颗、备用 0 颗 → 投票轮 work=0 空转。
       §0.64 只补了「陡峭判据」的相对兜底，阈值本身仍是绝对值。
    2. **打分轮 y 镜像写死**：真实照片两种 parity 都有（旧照 +1、新照 −1），
       写死单向必然让另一类全体 0 分 → 旧照 bestScore 7→5 不过门槛（回归根因）。
       改为双 parity 拟合、取残差小者（正确 ~0.02° vs 错误 ~6°，分离 300 倍）。
    3. **打分轮胜出模型的投影中心不一致**：`expandHypothesisPairs` 沿用种子星均值
       当原点，而胜出模型已迭代到画面中心 → 宽场外推差出整个视场 → 24 内点解作废
       （5087/5092 实测）。展开前补同款原点迭代。
    4. **赤经跨 0° 接缝**：`fitPairs` 用 RA 算术平均，跨接缝配对（345.5°/2.1°/3.3°）
       平均出 117° → tanXY 全 NaN → 拟合失败。改球面向量平均。
  - **附带的确定性修复（§0.71e）**：实测同代码跨 JVM 进程 SOLVED/UNSOLVED 翻转
    （演示照 5057/5087/5092、假阳性 apod5）。根因：`triKey` 未规范化 HIP 顺序
    （同一三角形多锚点重复记录，并列处按 HashMap 遍历序截断）+ 候选排序无决胜。
    规范化 triKey（索引同时缩小约 3 倍）+ 排序加 hip 决胜 → **4/4 次全量回归逐位一致**。
  - **验证**：新照片（原图/App 管线）inl=25、旧照片三种渲染 inl=25、gray12 **12/12**、
    窄场 apod4 SOLVED、假阳性 8 张全拒、单测 **165/165**。
  - 新增单测 `VoteThresholdScaleTest`（6 项，量纲无关性）。
  - 详见 `docs/69-验证报告增补-§0.71-修复9月17日照片识别失败与回归v1.5.60.md`。
  - **教训**：阈值「相对化」不能只做一半 —— 判据相对、阈值绝对，等价于没修。
    另外下限类阈值要**双向**约束：既要有相对下限（SEP 量纲），也要钉住绝对上限
    （亮照片 top 上万时，纯比例会把下限抬到 185/77，把真星整片切掉）。

- **2026-09-18（v1.5.59）** — **新增识别日志：失败原因与现场留存（§0.70，开发者需求）**：
  - **动机**：从 §0.43 起「用户说识别不了 / 开发机复现不出」反复出现，只能靠截图推断；
    而 §0.64 的调试转储**只在 debug 构建写盘**，用户装的是 release 包 —— 所以从未派上用场。
  - **做法**：新建 `SolveLogStore`，**release 也生效**。两类产物：
    `starcam-log.txt`（512KB 滚动人类可读日志）+ `fail-<时间戳>/`
    （`report.json` 结构化报告 + `input.gray.gz` 匹配器实际吃到的像素）。
  - **接入**：三个引擎分支全路径记录，成功也记一行摘要；失败时按「提星失败 /
    星点太少 / 有星点但未匹配（附投票轮+打分轮统计）/ 低内点被复核拒绝」**归因**。
    新增生产可观测字段 `debugVoteStats`（work/votedStars/topVote/avgMargin）——
    此前只在调试函数内部可见，它是区分「真信号缺失」与「阈值把关太严」的直接证据。
  - **UI**：失败页新增可折叠「技术细节」+ 一键复制；设置页新增日志开关/统计/清空/路径。
  - **容量控制**：像素现场 gzip 无损压缩，实测 13.8MB → **2.0MB（14.5%）**，
    10 份现场仅 20MB；日志文本 512KB 滚动截断。所有写入 try/catch 吞异常，
    **绝不干扰识别主流程**。
  - **验证**：新增 9 项日志单测（全绿）；全量 **154 项 / 16 类 / 0 失败**；
    照片回归 apod4 + 南宁照 SOLVED、6 张假阳性全 UNSOLVED（无回退）；
    release APK 签名 `b04a854f…` 与历史一致。
  - 详见 `docs/68-验证报告增补-§0.70-识别日志失败原因与现场v1.5.59.md`。
  - **遗留**：本机模拟器不可用（宿主机无虚拟化），日志的端到端行为需真机确认。


- **2026-09-17（v1.5.58）** — **修复官方引擎星点来源优先级（§0.69，用户报告）** +
  **构建环境全面迁回本机**：
  - **问题**：用户新拍的 74° 广角照片识别不出（与 §0.64 同类，但这次给了原图，
    本机直接复现）。astrometry.net 官方 `solve-field` 用同图**解出 nmatch=141
    / log-odds 765**，证明照片可解、天区不冷门（与旧南宁照中心相距仅 0.022°）。
  - **根因**：`astro_bridge.c` 的星点来源优先级是「**外部星点优先**」——只要 Kotlin
    侧传 ≥4 颗就完全跳过 simplexy。而 Kotlin 侧只传 **40 颗**（写死）。§0.65 修好
    simplexy 后，这条为绕开旧 bug 加的兜底逻辑**没撤掉**，于是 simplexy 提出的
    数千颗星被 40 颗挡在门外 → 广角欠曝照片的 mag 4.5~6 暗星全丢 → quad 建不起来。
    对照实验：simplexy 全量（4478 源）**solved** / `--objs 40` **did not solve**。
  - **修复**：桥接层改为「**simplexy 优先，外部星点仅兜底**」（两条路径都不泄漏
    `ext_field`）；Kotlin 侧兜底星点上限 **40 → 200**。
  - **验证**：simplexy 提星数 **3225**（host 实测，不再被阻断）；本机全量单测
    **145 项 0 失败**；照片回归 apod4 SOLVED（inl 11）、南宁照 SOLVED（inl 25）、
    6 张假阳性全 UNSOLVED（无回退）；`.so` 6 导出符号齐全；APK 本机构建成功。
  - **构建迁移（开发者要求「所有编译必须在本机」）**：实测打通关键前提 ——
    **Windows 版 NDK clang 可直接链接 Linux ELF 静态库**（无需 WSL/虚拟机）。
    把 astrometry 源码树头文件 + 8 个静态库 + cfitsio 交叉产物共约 35MB 迁到
    `C:\dev\astrometry-local\`，新增 `tools/build_so.bat`。实测本机：`.so` **<1 分钟**、
    APK **3 分 16 秒**、单测 **57 秒**（服务器需数分钟且多次 OOM）。
  - 详见 `docs/67-验证报告增补-§0.69-官方引擎星点来源优先级修复v1.5.58.md`。

- **2026-09-17（v1.5.57 发版）** — **修复 R8 缺注解类导致 release 构建失败**（§0.68，发版时发现）：
  - **现象**：v1.5.57 代码早已推送（`e63894f`），但一直**没发版** —— `apk/` 与
    GitHub Releases 最新都停在 v1.5.55。
  - **根因**：§0.66 引入 `EncryptedSharedPreferences`（androidx.security → Tink），
    Tink 的字节码引用 `com.google.errorprone.annotations.*` —— 这些是 **annotations-only
    的编译期注解**，不随运行时依赖发布。R8 的 "Missing classes" 检查把它当**致命错误**
    → `minifyReleaseWithR8` 失败。**爆炸半径：v1.5.56 与 v1.5.57 都打不出 release 包**
    （日常只构建 debug，所以一直没暴露）。
  - **修复**：`proguard-rules.pro` 追加 R8 自动生成的 `missing_rules.txt` 四条
    `-dontwarn com.google.errorprone.annotations.*`。注解只参与编译期静态检查，
    忽略安全。
  - **连带修正**：构建服务器上没有签名密钥（§0.66 去掉硬编码路径后三级查找都不命中）
    → `signingConfig = null` → release 产物**未签名**。把 jks + properties 放到服务器
    `~/.starcam/`（600 权限），正好命中 `build.gradle.kts` 第三级默认路径，无需改代码。
  - **顺带发现既存缺陷**：**GitHub 上 v1.5.55 的 release APK 是未签名的**
    （`apksigner` 报 DOES NOT VERIFY、无 `APK Sig Block 42`；对照 v1.5.53/49 均正常）——
    未签名包在 Android 上装不上。v1.5.57 已是签名包，用户升级即可绕开。
  - **构建服务器两个坑**（已记入 §0.68 供后续发版参考）：
    ① R8 默认/800m 堆会 **Java 堆溢出**（≠物理 OOM），实测
    `-Dorg.gradle.jvmargs="-Xmx1400m -XX:MaxMetaspaceSize=400m -XX:+UseSerialGC"`
    可用（release 3m13s 成功）；② `--rerun-tasks` 触发全量 Kotlin 重编译会被内核
    OOM-kill，**须分两步**：先让编译产物就绪，再跑测试（复用产物，45 秒完成）。
  - **验证**：145 项单测 / 15 类 / **0 失败**；release APK 16,520,558 bytes
    sha256 `856637d7…`；debug APK 26,915,653 bytes sha256 `e37fda26…`；
    签名 v2 scheme 通过、证书 `CN=StarCam` SHA-256 `b04a854f…`
    **与历史版本一致**（可覆盖升级）；本地 `apksigner` 独立复验通过。
  - 详见 `docs/66-验证报告增补-§0.68-R8缺注解类导致release构建失败v1.5.57.md`。

- **2026-09-17（v1.5.57，已发版）** — 索引内存可归还 + 求解线程状态实例化（§0.67）：
  - **前置修复**（§0.65 遗留）：`solve_done` / `solve_thread_leaked` 原是进程级
    单例，正确性依赖「solve 串行进入」。且有真实缺陷：`run_with_timeout` 每次
    进入都清零泄漏标志，若上次泄漏线程、本次正常结束，调用方就会对**仍被读取的
    内存**执行 `solver_free`（use-after-free）。现改为 job 堆分配 + out 参数返回
    本次泄漏状态，并加 `g_leaked_threads` 原子计数。
  - **新增 JNI 接口**：`releaseIndexes()` 归还 8 档索引（~11MB）；
    `indexCacheStats()` 返回 `{cached, loads, leakedThreads}`。释放前检查
    `g_leaked_threads == 0`，有 detach 线程在跑就拒绝释放（宁可占内存也不崩）。
  - **Kotlin 侧**：新增 `StarCamApplication`（manifest 注册），在
    `onTrimMemory(>= TRIM_MEMORY_RUNNING_LOW)` 时调 `trimIndexCache()`。
    阈值选 RUNNING_LOW 是避免用户连续识别时把索引丢掉导致下次变慢。
  - **顺带修**：`get_or_load_index` 缓存满 16 档时原静默 `return NULL`，现打
    stderr 说明原因（调用方原本无法区分「没打包索引」和「缓存满了」）。
  - **验证**（x86_64 主机，真实桥源码）六项全通过：释放 8 档 → cached 归 0 →
    再求解自动重建（loads 8→16）→ 并发 3 线程各返回不同 nstars
    （2076/1964/2049，证明 job 实例化生效）。arm64 `.so` 五个导出 + 两个新导出
    全部就位。
  - 回归：**145 项单测 0 失败**（40 + 105 两批）；12 张演示照 **12/12 TRUE-SOLVE**；
    南宁照 SOLVED（25 内点）。
  - **已知取舍**：`g_leaked_threads` 只增不减（detach 后无法观测线程何时结束），
    因此一旦发生过超时 detach，索引释放就永久失效。这是刻意的保守选择——
    宁可少还 11MB 也不 use-after-free。超时 detach 是罕见路径，正常识别不受影响。
  - 详见 `docs/65-验证报告增补-§0.67-索引内存可归还与线程状态实例化v1.5.57.md`。


- **2026-09-17（v1.5.56，待发版）** — 工程健壮性批次（§0.66）：
  - **凭据保护**：API Key 从普通 SharedPreferences 迁到
    `EncryptedSharedPreferences`（AES256-GCM + Keystore 主密钥）。带明文迁移
    （读时自动搬迁并删除明文项）与降级回退（Keystore 异常时用普通存储 + Log.w）。
  - **构建可移植**：签名凭据路径不再硬编码 `C:/dev/...`，改为
    `STARCAM_KEYSTORE` 环境变量 → `-PkeystoreProps` → `~/.starcam/...` 依次查找，
    都没有则降级未签名 APK。这是 CI 产可安装包的前置条件。
  - **回归自动化**：新增 `.github/workflows/ci.yml`（纯 JVM 单测 + gitleaks
    凭据扫描）。CI 用 `-PskipPhotoTests=true` 跳过依赖不入库素材的真值回归台；
    本机照常执行。保留 `--rerun-tasks` 以避开 FROM-CACHE 复用的坑。
  - **相机链路**：`ImageAnalysis` 从 `RGBA_8888`（读 4 平面逐字节拼 ARGB）改为
    `YUV_420_888` 只读 Y 平面 —— Y 就是亮度，省掉 3/4 数据搬运与 RGB→灰度转换；
    节流间隔按核数自适应（≤4 核 → 8s）。**真机未验证**。
  - **在线客户端**：强制 HTTPS（本地回环除外）、轮询指数退避 + `Retry-After`、
    `waitForJob` 超时提常量、网络错误重试一次、429/503 识别为服务繁忙、
    按 (路径,大小,mtime) 去重防连点浪费配额。
  - **调试开关收口**：6 个 `debugXxx` 全部收为 `internal` 并集中注释
    （原 `debugForceVoteThreshold` 是 public var，任何模块可写且无标注无日志）。
    字段名保持不变 —— 离线验证台用反射读写。
  - 验证：**单测 150 项 0 失败**；12 张演示照 **12/12 TRUE-SOLVE 0 MISS**；
    南宁用户照 SOLVED（25 内点）。
  - 详见 `docs/64-验证报告增补-§0.66-工程健壮性批次v1.5.56.md`。


- **2026-09-17（v1.5.55，待发版）** — **原生提星恒为 0 的根因修复**（§0.65）：
  - **推翻了 v1.5.54 的结论**：不是「部分 ARM64 机型 simplexy 数据竞争」。
    真正原因是三处确定性 C 层缺陷，在 x86_64 上 100% 复现，与 CPU 架构无关：
    ① `simplexy_set_defaults()` 内部是 `memset(整个结构体)`，而桥**先**填
       `image/nx/ny`、**后**调它 → 三个字段被清零 → `simplexy_run` 面对 NULL 图像；
    ② `simplexy_free_contents()` 会 `free(s->image)`，而 `s.image` 指向 JNI
       `GetFloatArrayElements` 返回的 **ART 堆**指针 → 非法 free（①把指针清成 NULL
       恰好掩盖了②，**必须同修**）；
    ③ `extract_stars_impl` 用 `if (rc != 0 || npeaks <= 0)` 判失败，而 `rc=0`
       （`dmask` 未标记任何超阈值像素）**才是**失败 → 判据恒真 → SEP 路径永远返回
       `extraction-failed`，Kotlin 侧「SEP 优先」的三级降级从未生效。
  - **证据**：同一张真图只改字段填写顺序 → 错误顺序 `image=(nil) nx=0 ny=0, npeaks=0`；
    正确顺序 `npeaks=2063`。用真实 `astro_bridge.c` + 模拟 ART 堆的 JNI 替身编译成
    x86_64 主机测试，A/B 对照：修复前 `ok=0 n=0`（6 张全部），修复后 `ok=1 n=200`
    （6 张各跑 2 遍），AddressSanitizer 零报告。
  - **附带修复**：`maxStars` 按通量降序取前 k（原按扫描序截断，丢失下半张图亮星）；
    `thresholdBgMultiple` 映射到 `plim`（原被完全忽略）；JNI 数组长度校验泄漏；
    超时三态判定（`pthread_create` 失败原被 `else if (timedout)` 当成超时）；
    `join` 加 5s 宽限期 + detach 保护（`solve_thread_leaked` 时跳过 `solver_free`，
    宁可泄漏也不崩）；`solve_job_t` 从栈移到静态（detach 后栈失效）；
    核实 `quit_now` 确实被上游 solver 轮询（`solver.c` 749/970/1022/1120/1259），机制有效；
    新增 `release_simplexy()` 只释放 simplexy 自有数组、不动调用方的 `image`。
  - **源码树重建**：服务器 astrometry.net 0.97 源码树曾被清空，本次从官网重新获取
    （md5 `cf8ae4ce158af7c9867209796bceea9c`）并固化；全新树需额外
    `make config`（生成 `os-features-config.h`）与 `gsl-an` 独立 configure
    （否则 `sys/infnan.c` 报 `cannot define gsl_finite`）；构建顺序修正为
    `gsl-an → qfits-an → util → libkd → catalogs → solver → bridge`。
    产物 `libstellar_solver.so` 5,063,768 B，反汇编确认 `set_defaults` 在 `run` 之前、
    且不再引用 `simplexy_free_contents`；APK 内 `.so` md5 与源码树一致。
  - 验证：全量单测 **150/150 0 失败**；12 张演示照 **12/12 TRUE-SOLVE, 0 MISS**；
    南宁用户照 SOLVED（ra=0.689 dec=23.192, 25 内点，真值窗口内）。
  - 详见 `docs/63-验证报告增补-§0.65-原生提星恒为0根因修复v1.5.55.md`。

- **2026-09-14（v1.5.54，待发版）** — 暗星检测能力提升与失败诊断转储（§0.64，用户报告）：
  - 现象：用户手机拍的星空原图识别不了，但**相册把对比度拉到最高就能识别**。
  - **本地无法复现**：七种复现路径（原图 / 精确复刻 app 解码链路 /
    检测星数 30~80 / 对比度 ×1~×3 / 直方图拉伸 / 横竖版 / SEP flux 量级模拟）
    全部稳定解出（内点 24~25）；检出前 40 颗里 39 颗是真星（97.5%）。
    → 判定为**真机环境特有差异**，需要真实像素数据定位。
  - 从截图确定的事实：显示"检测到 40 个星点"= `detectStars` 的旧默认上限，
    而 SEP 路径上限是 200 → 证明真机上 **SEP 提星失败并回退到 box-blur**
    （与 §0.20 已知问题一致：部分机型 ROM 上 .so 内 simplexy 提星 0 颗）。
  - 落地两项确定收益的改进：
    ① **检测上限 40 → 100**（实测演示照内点提升：5040 37→51、5092 19→24、
       5049 19→23；上限不宜过 150 —— apod5 假阳性对照在 n≥150 会被噪声星
       凑出 6 内点假解）；
    ② **投票阈值量纲兼容**：原"≥900 的星数"判据按 box-blur 亮度量级标定，
       而 SEP 喂入的是 simplexy 的 flux（净流量，量级小得多），绝对判据恒不成立；
       补相对形态判据（第 20 亮星 / 最亮星 ≥ 0.55），与量纲无关。
  - **新增失败诊断转储** `StellarSolverNative.dumpLocalFail()`（仅 debug 构建）：
    本地匹配失败时落盘匹配器实际吃到的像素（`local_fail_latest.gray`，与测试台
    同格式可直接重放）+ 检出星点列表（`.txt`）。此前只有官方引擎失败会转储
    （§0.28），本地匹配器失败无任何数据留存 —— 这正是本次无法定位的原因。
  - 验证：全量单测 **150 项 0 失败**；12 张演示照 `true=12 false=0 miss=0`；
    6 张假阳性对照保持 UNSOLVED。
  - 详见 `docs/62-验证报告增补-§0.64-检测上限与量纲兼容v1.5.54.md`。
- **2026-09-14（v1.5.53，待发版）** — 修复**叠加星座连线漂移**（§0.63，用户报告）：
  - 现象：照片已能识别，但连线相对星点整体偏移、端点落不到星上。
  - 量化（对比 nova 真值）：平均偏差 6.0px、最大 23.0px、比例尺偏 0.92%；
    偏移呈"中心近零、边缘最大"形态 → 比例尺/畸变型系统误差。
  - 根因：匹配模型是「像素→切平面」的相似变换，而照片是关于**图像中心**的
    gnomonic 投影 —— 只有切平面原点=图像中心时才严格成立。原实现把原点取在
    **星表星平均 RA/Dec**（宽场下可偏画面中心数度），整场因此带上相似变换
    吸收不掉的畸变。真值对照实验：原点=星均值时误差 14.13px、原点=图像中心
    时 0.16px。另发现 `solve.ra/dec` 用 `tanInverse(tx,ty)` 算中心，
    与 WCS 表达的中心差 0.38°/1.07°，结果页坐标显示同样偏移。
  - 修复：① 切平面原点迭代到图像中心（4 轮收敛）；② `refineWcs(fixOrigin=true)`
    钉住原点（否则 GN 会把原点优化跑，等于没修）；③ `solve` 中心改由 WCS 反推，
    与渲染同一来源。
  - 效果：平均偏差 6.0→1.7px、最大 23→4.0px、比例尺误差 0.92%→0.24%、
    **内点 15→25**；apod4 与 12 张演示照片无回归、6 张假阳性仍全部拒绝；
    全量单测 150 项 0 失败。
  - 详见 `docs/61-验证报告增补-§0.63-修复叠加连线漂移v1.5.53.md`。
- **2026-09-14（v1.5.52，待发版）** — 修复 v1.5.51 引入的**识别耗时回归**（§0.62 补，用户报告）：
  - 现象：离线演示的 12 张照片超过 1 分钟未出结果。
  - 定位：匹配器本身正常（12 张在 JVM 台全部 3~4 秒解出），开销在**失败路径**——
    打分轮逐个评分 70398 个候选，每次遍历整个亮星域并做三角函数 → +15.5 秒；
    这些演示照片无 EXIF 焦距，app 计划为 [内置星表, 官方引擎]，
    内置星表 ~25s 失败后再进官方引擎盲解两段（16s+12s）→ 总计超 1 分钟。
  - 修复：① 单位向量预算表（点积代替三角函数，**必须与 tanXY 同尺度**，
    误用 atan2 会在 74° 宽场精度崩溃）；② 评分轮 3 秒时间预算；
    ③ 高置信提前退出（阈值须明显高于判定门槛，否则选到次优解）。
  - 效果：失败路径额外开销 +15.5s → +2.5~3.0s；用户照片精度不变（inl=15）；
    12 张演示照片全部 TRUE-SOLVE（1.2~2.3s）；全量单测 150 项 0 失败。
- **2026-09-14（v1.5.51，已发布）** — 修复**宽场（60°+）照片识别失败**（§0.62，延续用户报告）：
  - 现象：用户南宁 74° 秋季四边形照片，§0.61 修好官方引擎的 FOV 先验后，
    **自研引擎仍 UNSOLVED**；此前误判为"星表覆盖不足"。
  - 独立真值（astrometry.net nova 解算）：RA 0.676° / Dec 23.204° / 121.6″/px @2200，
    logodds 1806、nmatch 424。按此投影内置星表：**该天区 mag≤4.0 有 25 颗、检测到 24 颗**
    —— 星表覆盖充足，"星表太浅"结论被推翻。
  - 真根因：逐星投票在宽场被伪三角形淹没（真 HIP 得 4~11 票，伪 HIP 得 14~19 票胜出）。
    量化：真三角形仅 26% 落入严格比值窗（±0.012），因为照片是 gnomonic 投影、
    像素距离 ∝ tan θ 而索引存纯角距；该失真在 34° 窄场仅 0.006，只在 60°+ 暴露。
  - 修复：新增**宽场打分轮** —— 投票全败时，对每个候选三角形做三点相似拟合，
    把全部星表星逆映射回像素做**一对一**匹配，按**对齐率**（aligned/inFrame）判决
    （真解 0.27 vs 伪解 0.05~0.11，取门槛 0.20），胜出后展开对应集交原 RANSAC 流程。
  - 验证：用户照片 SOLVED（RA 0.668 / Dec 23.194 / 122.75″ / 75.0° / 15 内点，
    与 nova 真值差 <0.01°）；apod4 仍 SOLVED；apod1/2/3/5、pleiades、m44×2 全部 UNSOLVED；
    全量单测 **150 项 0 失败**（新增 `RealPhotoMatchTest` 用户照片真值断言）。
  - 详见 `docs/60-验证报告增补-§0.62-宽场打分轮修复74度照片识别v1.5.51.md`。
- **2026-09-13（v1.5.50，待发版）** — 修复**竖拍照片 FOV 先验错配**（§0.61，用户报告）：
  - 现象：竖拍（EXIF Orientation=6/8）+ EXIF 有焦距的手机星空照片，官方引擎必败、
    自研引擎在秋季四边形等亮星稀疏天区也失败；对照 App 1s 解出。
  - 根因：`FovEstimate.fovDegFromFocal35(26, portrait=true)` 竖拍分支用 24mm 短边
    计算长边视场（49.55°），而传感器长边视场物理上恒为 67.3°（4:3 对角等效）；
    App 解码链路又不应用 EXIF 旋转 → 官方引擎按位图宽度 2200px 换算的 pixscale
    先验 [60.8, 109.5]"/px 恰好把真实值 110.1"/px 排除在外。
  - 修复：竖拍分支删除，长边视场恒按 36mm 计算（portrait 参数保留仅为兼容）。
  - 复现：用户照片 .gray 用项目自身匹配器 40/64/200 星全 UNSOLVED（本地复现）。
  - 单测更新：`StarSolverTest.fovFromFocal35_portraitSameAsLandscape`。
  - 详见 `docs/59-验证报告增补-§0.61-修复竖拍照片FOV先验错配v1.5.50.md`。
- **2026-09-13（v1.5.49 已发版）** — 构建并发布 v1.5.49：
  - 恢复 `testdata/` 回归素材（从 `C:\star\_archive\20260912-pre-merge\` 归档拷回），
    全量单测 **149 项 0 失败**（此前的 3 项素材失败随之消除，真实照片回归通过）；
  - debug 包（24.8MB）与 release 签名包（16.4MB，R8）均构建成功，
    签名证书与历史版本一致（CN=StarCam，SHA-256 `b04a854f…`，可覆盖升级）；
  - ⚠️ **本机 release 构建的两个坑**（本次实测）：① Gradle 配置缓存序列化
    `lintVitalAnalyzeRelease` 会尝试联网解析 `org.codehaus.groovy:groovy:3.0.21`，
    而本机 **Maven Central 不可达**（阿里云镜像可达），导致 release 构建失败；
    ② 解法为 `gradle :app:assembleRelease -x lintVitalRelease --offline`
    （debug 构建不受影响，正常联网命令即可）。
  - README 中英两版补 v1.5.49 说明并校正过期数字（测试 143→149、验证报告 48→50 份、
    章节至 §0.60、英文版梅西耶 45→44）；APK 归档到 `apk/`。
- **2026-09-12（v1.5.49 功能开发）** — 两项改动：
  - **§0.59 无 GPS 照片位置兜底**：照片缺 EXIF GPS 时用设备当前定位生成天区先验，
    官方引擎不必再从全天空盲解起步。三条硬边界——EXIF 定位永远优先、无拍摄时刻则不兜底、
    半套 GPS 视为缺失；位置来源在求解进度文案与结果页信息面板明示。
    日月行星标注（§0.58）与批量导出同步受益。
  - **§0.60 失败诊断图清晰度修复**（开发者报）：诊断图原先固定降采样到长边 800px，
    而原图可达 2200px，查看器放大后明显更糊；改为与原图同分辨率输出，圈半径 / 描边等比放大。
  - 单测 149 项（新增 `ExifPriorsFallbackTest` 6 项）。详见 `docs/57`、`docs/58`。
- **2026-09-12** — 把本机两份**无共同祖先**的副本（`C:\starword` 与 `C:\star\StarCam`）
  合并为**唯一工作区**：接入 GitHub 完整历史（6 提交 + `v1.5.26/37/39/48` 四个轻量 tag + origin），
  并吸收此前只存在于 StarCam 侧的 8 处修复 ——
  `WcsTransform.rescaledFor()` 的 CRPIX/CD 缩放修正（星图错位根因）、`ResultScreen`/`OverlayRenderer`
  改用它、`allowBackup=false`、在线求解上传副本清理、分析帧位图回收、OkHttp `use{}`、CD 缩放回归测试。
  旧副本与冗余归档在 `C:\star\_archive\20260912-pre-merge\`。
  新增 `AGENTS.md`（AI 工具统一入口）与 `交接说明.internal.md`（内部稿，不入库）；校正 `交接说明.md` §十。
- **2026-09-11** — v1.5.48 发布到 GitHub Release（APK 附件 + 轻量 tag）。
- **2026-09-10** — v1.5.48 月亮与行星实时标注（§0.58，`docs/56`）；仓库首次纳入 git。

## 三、阻塞 / 待开发者处理

- [x] ~~**`.github/workflows/ci.yml` 需手动添加到 GitHub**~~ —— **已解决**
      （2026-09-17）：用户在 GitHub 上给 token 补了 `workflow` scope，推送成功。
      顺带发现并解决了两件事：
      ① **网络**：本机 github.com 间歇不可达，已用「经构建服务器中转的 SOCKS
         隧道」打通（`_socks_proxy.py`，仅监听回环 1080）。推送用
         `git -c http.proxy=socks5h://127.0.0.1:1080 push`。
      ② **CI 自身缺陷**：首跑失败于 `android-actions/setup-android@v3`
         （runner 上报 "Wrong version in preinstalled sdkmanager" 后
         `sdkmanager tools` exit 1）。该 action 多余——GitHub runner 本就预装
         Android SDK。已删除并直接指向预装 SDK；同时补上缺失的 Gradle wrapper
         （仓库此前没有 wrapper，CI 无 gradle 可用）。
      **现状**：CI 全绿（run #2，145 项单测 0 失败 + gitleaks 通过）。
- [x] ~~**astrometry.net API key 轮换**~~ —— 已核实为误报：`apiKey` 只是
      `PlateSolveClient` 的入参名，仓库（含首发提交 `511c825`）里没有任何 key 字面值，
      `settings.hasApiKey` 也只是用户输入的开关。无需轮换。
- [ ] **真实照片回归素材不在工程内** —— `RealPhotoMatchTest` / `Photo12RegressionTest` 依赖
      `testdata/realphotos` 与 `testdata/gray12`，这批素材目前在归档目录
      （`C:\star\_archive\20260912-pre-merge\starcam-bundle\testdata\`），导致本机跑不了真实照片回归。
      建议恢复到 `C:\starword\testdata\`（`.gitignore` 已排除 `testdata/`，无入库风险）；
      临时替代：跑测试时用 `PHOTO_DIR` / `PHOTO12_DIR` 指向归档。
- [ ] **AR 视场/地平线真机复测**（需实体设备）。
- [ ] 中间提交 `f14b268`（WIP 重建）自身不编译，HEAD 已修复。
      如需整理历史：**本环境 `git rebase` 有删除 `.git` 的前科，务必先备份**。

## 四、与远端的关系

本地 `main` 领先 `origin/main`（未推送的提交会在这里累积）。**本文不写死数字**，以命令为准：

```bash
cd C:/starword
git fetch origin                      # 若提示不认识 origin/main，先执行这句（新克隆的目录通常已有）
git status -sb                        # 看 ahead / behind
git log --oneline origin/main..HEAD   # 看待推送的具体提交
```

推送前请确认：本地是否还有不想公开的内容（本机凭据路径、内部稿、机器相关配置）。

---

## 更新规约

1. 每次实质工作结束（改代码 / 定方案 / 发版 / 合并）后更新本文件：
   **补「最近完成」，并据实调整「下一步」**。
2. 「最近完成」保留最近 3~5 条即可，更早的删掉（历史在 `git log` 与 `docs/` 里）。
3. 只写**状态与决策**，不写过程细节；过程细节进 `docs/NN-验证报告增补-§0.xx-主题.md`。
4. 本文件与 `AGENTS.md` 一样，是**跨工具接力**的载体，务必提交进 git。
